This script is written in Bash, and automatically creates .keep files in empty directories.

Make sure that the flatkeep.sh file is in the environment you are currently working on.

How to use Flatkeep:
> bash flatkeep.sh
